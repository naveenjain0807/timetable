# Tasks for 2026-07-22

## Overview
This project is now a initialized Git repository and the initial starter code has been pushed to GitHub. The next focus is converting the Vite + React template into a timetable application with a strong developer workflow and an initial UI.

## Task list

1. Run the app locally and verify tooling
   - Priority: High
   - Estimate: 30 minutes
   - Description: Install dependencies, launch `npm run dev`, confirm the app loads in the browser, and verify there are no build or TypeScript issues.

2. Add GitHub workflow support
   - Priority: High
   - Estimate: 45 minutes
   - Description: Create a `.github/workflows` CI workflow to build the app on push, catch TypeScript or Vite build regressions, and establish a stable repository process.

3. Replace the placeholder template UI with a timetable skeleton
   - Priority: High
   - Estimate: 2 hours
   - Description: Update `src/App.tsx` to display a timetable grid for weekday/time blocks, include placeholder rows/columns, and make the layout ready for schedule content.

4. Define timetable state and schedule model
   - Priority: Medium
   - Estimate: 2 hours
   - Description: Introduce a TypeScript model for schedule entries, store timetable data in React state, and wire a simple in-memory add/edit flow.

5. Build responsive styling and accessibility
   - Priority: Medium
   - Estimate: 2 hours
   - Description: Improve `src/App.css` and `src/index.css` for a responsive timetable layout, ensure keyboard focus works, and add accessible labels for schedule controls.

6. Add a timetable entry form or filter controls
   - Priority: Medium
   - Estimate: 2 hours
   - Description: Implement a small UI for adding entries or filtering the timetable by day/subject, giving the app a useful first interaction.

7. Review linting and project quality settings
   - Priority: Low
   - Estimate: 1 hour
   - Description: Verify `.oxlintrc.json`, decide whether to enable type-aware lint rules, and ensure the project uses consistent formatting and checks.

## Notes
- Git repo initialized and the first commit has been pushed to `origin/main`.
- `.github` folder created; next step is to add workflow configuration for CI.
- The app still contains default Vite template content and needs timetable-specific UI work.
