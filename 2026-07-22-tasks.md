# Tasks for 2026-07-22

## Overview
This project is currently a fresh Vite + React + TypeScript starter app. The next priority is turning it from the template into a timetable/scheduling application with a working UI, structured data model, and stable development workflow.

## Task list

1. Initialize repository and confirm project state
   - Priority: High
   - Estimate: 30 minutes
   - Description: Verify whether this folder should be a git repo, initialize git if needed, commit the baseline Vite starter, and connect to the intended remote repository if available.

2. Run the app locally and verify tooling
   - Priority: High
   - Estimate: 45 minutes
   - Description: Install dependencies, start the Vite dev server, confirm the app loads in the browser, and verify there are no build or TypeScript issues.

3. Replace the placeholder template UI with a timetable skeleton
   - Priority: High
   - Estimate: 2 hours
   - Description: Create a basic timetable page in `src/App.tsx` with a grid layout for days/times, placeholder cells, and primary navigation for schedule views.

4. Define timetable state and data modeling
   - Priority: Medium
   - Estimate: 2 hours
   - Description: Add a TypeScript data model for schedule items, store events in React state, and implement loading/in-memory editing of timetable entries.

5. Build responsive styling and accessibility improvements
   - Priority: Medium
   - Estimate: 2 hours
   - Description: Update `src/App.css` and `src/index.css` for a responsive timetable layout, ensure labels and buttons are accessible, and support mobile/tablet screens.

6. Configure linting and project quality rules
   - Priority: Medium
   - Estimate: 1 hour
   - Description: Review `.oxlintrc.json`, add type-aware linting if needed, and ensure the project is ready for consistent code quality checks.

7. Add a small feature for timetable input or filtering
   - Priority: Low
   - Estimate: 2 hours
   - Description: Implement one user-facing enhancement such as adding new schedule items, filtering by subject or day, or toggling between weekly and daily views.

## Notes
- No `.git` repository metadata was detected in the current workspace folder.
- The starter app currently uses default Vite/React content and needs domain-specific timetable development next.
